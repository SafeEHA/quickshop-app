version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install – no package.json"
  build:
    commands:
      - echo "Build step not needed – just packaging files for CodeDeploy"

artifacts:
  files:
    - '**/*'
  discard-paths: no
